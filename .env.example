# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_API_VERSION=2024-02-01

# Model Deployment Names
# Use the deployment names from your Azure OpenAI resource
# Note: Using gpt-5 models version 2025-08-07 (latest stable)
GPT_5_CHAT_MODEL=gpt-5-chat    # Recommended: Fast, consistent output (128K context)
GPT_5_MODEL=gpt-5              # Optional: Balanced reasoning (400K context)
GPT_5_MINI_MODEL=gpt-5-mini    # Optional: Cost-effective reasoning (400K context, 5x cheaper)
